# Core
numpy>=1.26
torch>=2.2
gymnasium>=0.29
pettingzoo>=1.24

# Utilities
networkx>=3.2        # map analysis / plotting only, never in the hot path
pyyaml>=6.0
tqdm>=4.66

# Evaluation & logging
trueskill>=0.4.5     # 3-player ratings
matplotlib>=3.8
tensorboard>=2.16
scipy>=1.11          # chi-square seat-symmetry test, CIs

# Dev
pytest>=8.0

# Optional (uncomment when needed)
# wandb
# torch-geometric   # only for the GNN encoder upgrade (M4+); MLP baseline first
